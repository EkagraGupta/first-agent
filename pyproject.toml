[project]
name = "first-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.13.0",
    "bitsandbytes>=0.49.2",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
    "torch>=2.11.0",
    "transformers>=5.5.3",
]
